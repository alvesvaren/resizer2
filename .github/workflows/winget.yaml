name: Publish to WinGet
on:
  release:
    types: [released]
jobs:
  publish:
    runs-on: windows-latest
    steps:
      - uses: vedantmgoyal9/winget-releaser@main
        with:
          identifier: AlveSvaren.Resizer2
          installers-regex: 'setup\.exe$' # Only .exe files
          token: ${{ secrets.WINGET_TOKEN }}
